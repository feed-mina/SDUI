spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/SDUI_TD
    username: postgres
    password: "1234"
  data:
    redis:
      host: localhost
      port: 6379
    openai: 
      api-key: ${OPENAI_API_KEY}
